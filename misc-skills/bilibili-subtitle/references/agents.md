# Agent Configuration

This repository is BBDown-only. It does not configure non-BBDown processing providers.

Parent skills should call the CLI contract in `references/contract.md` and handle the transcript outputs directly.
